#include <iostream>
#include <vector>
#include <ctype.h>
#include <stdlib.h>

int main(int argc, char ** argv)
{
    int opt;
    while ((opt = getopt(argc, argv, "p:")) != EOF)
    {
        
    }
}